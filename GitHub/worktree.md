## Worktree
```bash
git worktree add ../main-fix main

git worktree add -b branch ../branch-fix  # + new branch

git worktree list

git worktree remove ../main-fix 

----
git worktree lock ../main-fix  # or unlock

```